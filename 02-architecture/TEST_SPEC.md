# TEST_SPEC.md — Named Test-Case Catalog (taskq-api, Round 2/3)

> **Phase 2 deliverable.** Generated by Agent A (ARCHITECT) following the
> `derive_test_cases.md` v1.1 skill protocol. Test names from P1
> `TEST_INVENTORY.yaml` are preserved verbatim where specified (see Step 0
> table below). This is the SINGLE SOURCE OF TRUTH for all test traceability
> checks at Gates 1-4. Validated by Agent B (TECH_LEAD) before P2 exit. P3
> Agent A implements tests FROM this catalog — not ad-hoc.

**Project**: `taskq-api`
**SRS version**: `01-requirements/SRS.md`, FR Block `created_at: 2026-09-07` (22 requirements: FR-01..FR-10, NFR-01..NFR-12)
**SAD version**: `02-architecture/SAD.md`, SAB Block `created_at: 2026-09-07`, `phase: 2`
**TEST_INVENTORY.yaml version**: `format_version: "1.1"`, 13 declared test_inventory entries (10 FR + 2 NFR + 4 illustrative-cross_cutting placeholders TC-N02-01/TC-N09-01 reused below, plus TC-FR01-02/example-unit)
**Generated**: 2026-09-07
**Active NFR Patterns**: NP-01, NP-02, NP-03, NP-04, NP-06, NP-07, NP-08, NP-09, NP-10, NP-12, NP-13, NP-15 (12 of 15 — see table below for the 3 not activated and why)

---

## Step 0: TEST_INVENTORY.yaml Naming Authority — names preserved verbatim

| TEST_INVENTORY tc_id | fr/nfr | test_function (used verbatim below) | Case # |
|---|---|---|---|
| TC-FR01-01 | FR-01 | `test_fr01_example_integration` | superseded — see note¹ |
| TC-FR01-02 | FR-01 | `test_fr01_example_unit` | superseded — see note¹ |
| TC-FR02-01 | FR-02 | `test_fr02_run_task_returns_202_with_run_id` | FR-02 case 1 |
| TC-FR03-01 | FR-03 | `test_fr03_missing_api_key_returns_401` | FR-03 case 2 |
| TC-FR04-01 | FR-04 | `test_fr04_insufficient_scope_returns_403` | FR-04 case 2 |
| TC-FR05-01 | FR-05 | `test_fr05_rate_limit_exceeded_returns_429` | FR-05 case 2 |
| TC-FR06-01 | FR-06 | `test_fr06_session_commits_on_success_rolls_back_on_exception` | FR-06 case 1 |
| TC-FR07-01 | FR-07 | `test_fr07_migration_round_trip_preserves_data` | FR-07 case 1 |
| TC-FR08-01 | FR-08 | `test_fr08_graceful_drain_waits_for_inflight_tasks` | FR-08 case 1 |
| TC-FR09-01 | FR-09 | `test_fr09_readyz_returns_503_when_db_down` | FR-09 case 3 |
| TC-FR10-01 | FR-10 | `test_fr10_error_response_is_problem_json` | FR-10 case 1 |
| TC-N02-01 | NFR-02 | `test_security_example` | Deferred #1 (layer: static) |
| TC-N09-01 | NFR-09 | `test_deployment_example` | not deferred (layer: integration) — superseded by concrete FR-07/NFR-09 disposition below |

¹ `test_fr01_example_integration` / `test_fr01_example_unit` are illustrative placeholder names from the P1 template (not FR-01-specific behavior). Per NAMING RULE v2.6.1 item 3, the full Q1-Q8 protocol was still executed for FR-01, generating the concrete named cases below (`test_fr01_create_task_returns_201`, etc.); the placeholder names are not reused as they carry no derivable behavior of their own. `test_deployment_example` is likewise a placeholder; NFR-09's real disposition is FR-07 case 9 (real-SQLite, no skip) + the Deferred-table static checks (`test_pytest_run_reports_zero_skipped` etc.) below.

---

## NFR Pattern Activation (Step 1 + Step 1b + Step 1c Output)

| Pattern | Trigger (SRS keyword / SAD module / SEC threat) | Activated |
|---------|--------------------------------------------------|-----------|
| NP-01 (auth 401)          | SRS: "authentication" (SRS.md §1, FR-03) + SEC: T-01 | ✅ |
| NP-02 (authz 403)         | SRS: "authorization" (SRS.md §1, FR-04) + SEC: T-02 | ✅ |
| NP-03 (rate limit 429)    | SRS: "rate limit" / "throttle" (FR-05) + SEC: T-05  | ✅ |
| NP-04 (validation 422)    | SRS: "input" validation (FR-01 AC-1.5, pydantic `TaskCreate`) | ✅ |
| NP-05 (idempotency)       | not found — no "idempotent" / "dedup" / "at-least-once" keyword anywhere in SRS.md | ❌ |
| NP-06 (latency SLA)       | SRS: "p95" (NFR-01 AC-N1.1/AC-N1.2)                 | ✅ |
| NP-07 (dependency fault)  | SRS: "fault" (NFR-03 AC-N3.4, DB connection failure) + SAD: `repository.session` (`pool_pre_ping`, `/readyz` DB check) | ✅ |
| NP-08 (security attack)   | SRS: "security" (NFR-02) + SEC: T-03 (tampering, shell metacharacters) + SEC: T-04 (information_disclosure) | ✅ |
| NP-09 (audit log)         | SRS: "log" / "trace" (FR-10 `correlation_id`) + SEC: T-06 (repudiation) | ✅ |
| NP-10 (data round-trip)   | SRS: "persist" / "round-trip" (FR-07 AC-7.3)        | ✅ |
| NP-11 (backward compat)   | The only literal keyword hit is "migration" (FR-07), which is Alembic **schema** migration (already the whole subject of FR-07), not a prior-delivery-phase API contract. `taskq-plus` (Round 1) is a single-process CLI with no HTTP surface — there is no phase-N-1 API contract for `taskq-api` to preserve. Not activated; the Backward Compatibility Cross-Cutting subsection is omitted per Step 4 ("present only for multi-phase projects"). | ❌ |
| NP-12 (pagination)        | SRS: "limit" / cursor (FR-01 AC-1.3, cursor-based pagination) | ✅ |
| NP-13 (concurrency)       | SRS: "async" (FR-08 `asyncio.TaskGroup`) + SAD: `repository.rate_repo` (shared `rate_buckets` row, row-level lock — TB-05) | ✅ |
| NP-14 (encryption)        | not found — no "encryption" / "TLS" / at-rest-encryption keyword anywhere in SRS.md (the one substring hit is inside an unrelated field name, not a real keyword) | ❌ |
| NP-15 (timeout)           | SRS: "timeout" (FR-02/FR-08, `TASKQ_TASK_TIMEOUT`) + SAD: `service.runner` (external subprocess) | ✅ |

Step 1b Architecture-Risk Triggers applied (regardless of SRS keywords, forced from SAD module traits):

| Module risk trait | Module | Forced pattern | Owning FR(s) |
|---|---|---|---|
| shared mutable state, row-level lock, concurrent access | `taskq_api.repository.rate_repo` | NP-13 | FR-05 |
| external process (`asyncio.create_subprocess_exec`) | `taskq_api.service.runner` | NP-15 | FR-02, FR-08 |
| network client to external system (DB), health/retry semantics | `taskq_api.repository.session` | NP-07 | FR-06, FR-09 |

No cache / optional-dependency module exists in this round's SAD (no NP-07-via-cache trigger; the NP-07 activation above is driven entirely by the DB dependency, not a cache).

Step 1c SEC-Block Threat Triggers applied (from SAD.md §6, `applicability: full`):

| Threat | Category | Forced pattern | Owning FR (via `owner_module`) | Case (=`verified_by`, verbatim) |
|---|---|---|---|---|
| T-01 | spoofing | NP-01 | FR-03 (`service.auth`) | `test_sec_t01_invalid_or_revoked_api_key_rejected` |
| T-02 | elevation_of_privilege | NP-02 | FR-04 (`api.dependencies`) | `test_sec_t02_insufficient_scope_rejected_without_leaking_existence` |
| T-03 | tampering | NP-04 + NP-08 | FR-02 (`service.runner`) | `test_sec_t03_shell_metacharacters_not_interpreted` |
| T-04 | information_disclosure | NP-08 + NP-14* | FR-10 (`api.error_handlers` → `redaction`) | `test_sec_t04_secrets_redacted_from_response_and_logs` |
| T-05 | denial_of_service | NP-03 | FR-05 (`repository.rate_repo`) | `test_sec_t05_concurrent_requests_do_not_exceed_burst` |
| T-06 | repudiation | NP-09 | FR-10 (`api.error_handlers`) | `test_sec_t06_correlation_id_present_and_logged` |

\* T-04's forced NP-14 (encryption) has no applicable case: the mitigation is line-redaction of secret-shaped substrings before write, not at-rest/in-transit encryption — SPEC.md declares no encryption requirement anywhere (confirmed by the NP-14 keyword scan above). NP-08 (the redaction/information-disclosure half of T-04) is fully covered by the same `test_sec_t04_…` case; NP-14 is recorded here as forced-but-inapplicable rather than silently dropped.

---

## Functional Requirement Test Cases

### FR-01: 任務資源 CRUD API (`/v1/tasks` create/read/list/delete)

**Classification**: API_ENDPOINT, DATA_ENTITY
**Active Patterns**: NP-04, NP-12 (NP-06 performance cases moved to the dedicated `### NFR-01` section below — they exercise this FR's endpoints but the NFR-01 dimension gate does not cover them, per SRS.md's *Coverage note*, so they get their own citable NFR section rather than being buried inside FR-01)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr01_create_task_returns_201` | command="echo hello"; name="build-report" | happy_path | Q1 |
| 2 | `test_fr01_get_task_returns_full_record` | task_id="a-previously-created-task"; precondition="a task with this id exists, created via case 1's flow" | happy_path | Q1 |
| 3 | `test_fr01_list_tasks_returns_200_with_items` | total_tasks="3" | happy_path | Step 2.5 |
| 4 | `test_fr01_delete_task_cascades_result_rows_in_same_transaction` | task_id="task-with-results"; precondition="task_id refers to a task that already has ≥1 task_results row" | happy_path | Q1 |
| 5 | `test_fr01_create_task_empty_command_rejected` | command="" | validation | Q2 |
| 6 | `test_fr01_create_task_injection_char_rejected` | command="echo hi; rm -rf /tmp" | validation | Q2 |
| 7 | `test_fr01_get_task_unknown_id_returns_404` | task_id="00000000-0000-0000-0000-000000000000" | failure | Q2 |
| 8 | `test_fr01_create_task_duplicate_name_returns_409` | name="dup-task"; command="echo hi"; precondition="a task named \"dup-task\" already exists" | failure | Q2 |
| 9 | `test_fr01_create_task_name_at_1000_chars_accepted` | name_length="1000" | boundary | Q3 |
| 10 | `test_fr01_create_task_name_above_1000_chars_rejected` | name_length="1001" | boundary | Q3 |
| 11 | `test_fr01_create_task_empty_name_rejected` | name=""; command="echo hi" | boundary | Q3 |
| 12 | `test_fr01_list_tasks_default_limit_is_50` | requested_limit="none"; total_tasks="60" | boundary | Q3 |
| 13 | `test_fr01_list_tasks_limit_at_200_accepted` | limit="200" | boundary | Q3 |
| 14 | `test_fr01_list_tasks_limit_above_200_rejected` | limit="201" | boundary | Q3 |
| 15 | `test_fr01_list_tasks_cursor_pagination_returns_next_page` | page_size="2"; total_tasks="5" | nfr_pattern | Q6/NP-12 |
| 16 | `test_fr01_list_must_not_use_offset_query_param` | query_params="status,limit,cursor" | negative_constraint | Q8 |
| 17 | `test_fr01_create_task_output_feeds_fr02_run_pipeline` | name="feed-task"; command="echo hi" | integration | Q7/FR-02 |

**Sub-assertions** (predicate over a case's declared Inputs / the production `result`):

| rule_id | predicate | applies_to |
|---|---|---|
| AC1-boundary-at-1000 | `name_length == "1000"` | 9 |
| AC1-boundary-above-1000 | `name_length == "1001"` | 10 |
| AC1-empty-name-rejected | `name == ""` | 11 |
| AC1-list-default-returns-50-of-60 | `len(result) == 50` | 12 |
| AC1-list-limit-at-200 | `limit == "200"` | 13 |
| AC1-list-limit-above-200 | `limit == "201"` | 14 |
| AC1-negative-no-offset-param | `"cursor" in query_params` | 16 |

Case 1 disposes AC-1.1 (`POST /v1/tasks` create + `TaskCreate` validation, → 201); cases 2 and 7 dispose AC-1.2 (`GET /v1/tasks/{id}` full record / unknown id → 404); case 4 disposes AC-1.4 (`DELETE /v1/tasks/{id}` cascades result rows in the same transaction); case 8 disposes AC-1.6 (duplicate name → 409).

Q4 (state transitions): N/A — a `tasks` row's `status` lifecycle belongs to FR-02 (the run endpoint), not FR-01's CRUD surface; explicitly not skipped without comment (per protocol, the reason is stated inline rather than the question being silently dropped).
Q5 (external dependency fault): N/A for FR-01 specifically — DB-fault handling is FR-06/FR-09's concern (own NP-07 cases below); FR-01's classification is DATA_ENTITY, not INTEGRATION.

---

### FR-02: 任務執行端點 (`POST /v1/tasks/{id}/run`, run history)

**Classification**: API_ENDPOINT, STATE_MACHINE, INTEGRATION
**Active Patterns**: NP-08 (Q6/1c, SEC: T-03), NP-15 (Q6/1b, SAD: `service.runner`)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr02_run_task_returns_202_with_run_id` | task_id="a-pending-task"; command="echo hi"; precondition="task_id refers to a previously created task in pending status" | happy_path | Q1 |
| 2 | `test_fr02_run_task_writes_task_results_row` | task_id="a-pending-task"; expected_exit_code="0" | happy_path | Q1 |
| 3 | `test_fr02_list_runs_returns_newest_first` | task_id="a-task-with-history"; run_count="3" | happy_path | Q1 |
| 4 | `test_fr02_run_task_unknown_task_id_returns_404` | task_id="missing-task" | failure | Q2 |
| 5 | `test_fr02_transition_pending_to_running_valid` | task_id="a-pending-task" | state_transition | Q4 |
| 6 | `test_fr02_transition_running_to_done_valid` | task_id="a-running-task"; exit_code="0" | state_transition | Q4 |
| 7 | `test_fr02_transition_running_to_failed_valid` | task_id="a-running-task"; exit_code="1" | state_transition | Q4 |
| 8 | `test_fr02_transition_running_to_timeout_valid` | task_id="a-running-task"; task_timeout_s="10.0" | state_transition | Q4 |
| 9 | `test_fr02_transition_done_to_running_rejected` | task_id="a-finished-task"; current_status="done" | state_transition | Q4 |
| 10 | `test_fr02_initial_state_is_pending` | task_id="a-newly-created-task" | state_transition | Q4 |
| 11 | `test_fr02_run_task_never_uses_shell_true` | command="echo hi" | negative_constraint | Q8 |
| 12 | `test_sec_t03_shell_metacharacters_not_interpreted` | command="echo hi; rm -rf /tmp/pwned"; subprocess_mode="in_process"; shared_TASKQ_HOME="false" | nfr_pattern | Q6/1c/NP-08 |
| 13 | `test_fr02_run_task_timeout_kills_subprocess_no_orphan` | command="sleep 60"; task_timeout_s="0.5"; subprocess_mode="in_process"; shared_TASKQ_HOME="false" | nfr_pattern | Q6/1b/NP-15 |
| 14 | `test_fr02_output_feeds_fr07_task_results_schema` | task_id="a-pending-task" | integration | Q7/FR-07 |

**Sub-assertions**:

| rule_id | predicate | applies_to |
|---|---|---|
| AC2-happy-command-nonempty | `len(command) > 0` | 1 |
| AC2-t03-command-has-shell-metachar | `";" in command` | 12 |
| AC2-timeout-shorter-than-sleep | `task_timeout_s == "0.5"` | 13 |
| AC2-invalid-transition-source-is-done | `current_status == "done"` | 9 |

Case 1 disposes AC-2.1 (202 + `run_id`); cases 11 and 13 dispose AC-2.2 (`asyncio.create_subprocess_exec`/no `shell=True`/`TASKQ_TASK_TIMEOUT`); cases 5-10 dispose AC-2.3 (the full `pending→running→done|failed|timeout` machine); case 2 disposes AC-2.4 (`task_results` row fields); case 3 disposes AC-2.5 (`GET .../runs` newest-first).

Cases 12-13 are Step-1b/1c-forced and live under `tests/integration/` (real subprocess, real timeout clock) per protocol §Step 1b.

---

### FR-03: API Key 認證 (`X-API-Key`, hashed storage)

**Classification**: SECURITY_CONTROL
**Active Patterns**: NP-01 (Q6, SRS + Q6/1c SEC: T-01)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr03_create_task_with_valid_write_key_returns_201` | api_key_scope="write"; command="echo hi" | happy_path | Q1 |
| 2 | `test_fr03_missing_api_key_returns_401` | api_key_header="absent" | nfr_pattern | Q6/NP-01 |
| 3 | `test_fr03_invalid_api_key_returns_401` | api_key_value="not-a-real-key" | validation | Q2 |
| 4 | `test_sec_t01_invalid_or_revoked_api_key_rejected` | api_key_revoked_at="2026-01-01T00:00:00Z" | nfr_pattern | Q6/1c/NP-01 |
| 5 | `test_fr03_api_key_stored_as_sha256_hash_never_plaintext` | key_plaintext="tq_live_abcdef1234567890" | happy_path | Q1 |
| 6 | `test_fr03_key_create_prints_plaintext_exactly_once` | scope="admin" | happy_path | Q1 |
| 7 | `test_fr03_healthz_and_readyz_exempt_from_auth` | endpoint="/healthz"; api_key_header="absent" | boundary | Q3 |
| 8 | `test_fr03_key_comparison_uses_constant_time_compare` | api_key_value="tq_live_validkeyvalue123" | happy_path | Q1 |

**Sub-assertions**:

| rule_id | predicate | applies_to |
|---|---|---|
| AC3-invalid-key-value | `api_key_value == "not-a-real-key"` | 3 |
| AC3-revoked-key-has-timestamp | `len(api_key_revoked_at) > 0` | 4 |
| AC3-plaintext-key-prefix | `key_plaintext.startswith("tq_live_")` | 5 |
| AC3-healthz-no-key-header | `api_key_header == "absent"` | 7 |

Cases 5 and 8 dispose AC-3.2 (SHA-256 hash storage + `hmac.compare_digest` constant-time comparison); case 6 disposes AC-3.3 (`key create` prints plaintext exactly once); case 4 disposes AC-3.4 (a revoked key is treated as invalid).

---

### FR-04: Scope 授權 (read/write/admin)

**Classification**: SECURITY_CONTROL
**Active Patterns**: NP-02 (Q6, SRS + Q6/1c SEC: T-02)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr04_admin_key_deletes_task_returns_200` | api_key_scope="admin"; task_id="a-previously-created-task" | happy_path | Q1 |
| 2 | `test_fr04_insufficient_scope_returns_403` | api_key_scope="write"; task_id="a-previously-created-task" | nfr_pattern | Q6/NP-02 |
| 3 | `test_sec_t02_insufficient_scope_rejected_without_leaking_existence` | api_key_scope="write"; task_id="does-not-exist" | nfr_pattern | Q6/1c/NP-02 |
| 4 | `test_fr04_scope_hierarchy_read_lt_write_lt_admin` | scope_order="read,write,admin" | happy_path | Q1 |
| 5 | `test_fr04_all_v1_routes_use_shared_scope_dependency` | route_count="7" | happy_path | Q1 |
| 6 | `test_fr04_write_scope_cannot_call_admin_metrics_endpoint` | api_key_scope="write"; endpoint="/v1/metrics" | validation | Q2 |

**Sub-assertions**:

| rule_id | predicate | applies_to |
|---|---|---|
| AC4-scope-hierarchy-order | `scope_order == "read,write,admin"` | 4 |
| AC4-403-existence-not-leaked-target | `task_id == "does-not-exist"` | 3 |
| AC4-route-introspection-count | `route_count == "7"` | 5 |

Case 4 disposes AC-4.1 (scope hierarchy `read<write<admin`); case 2 (with case 3's non-leak assertion) disposes AC-4.2 (insufficient scope → 403 without leaking existence).

Case 5's `route_count="7"` corresponds to the 7 `/v1/*` routes enumerated in SAD.md §2.3 (`POST/GET/{GET-list}/DELETE /v1/tasks(/{id})` = 4, `POST /v1/tasks/{id}/run`, `GET /v1/tasks/{id}/runs`, `GET /v1/metrics`) — `/healthz` and `/readyz` are exempt (FR-03 AC-3.5) and excluded from this count.

---

### FR-05: 流量控制 (token bucket rate limit)

**Classification**: ALGORITHM, INTEGRATION, SECURITY_CONTROL
**Active Patterns**: NP-03 (Q6, SRS + Q6/1c SEC: T-05), NP-13 (Q6/1b, SAD: `repository.rate_repo`)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr05_request_within_burst_capacity_allowed` | rate_burst="20"; rate_per_sec="5.0"; requests="20" | happy_path | Q1 |
| 2 | `test_fr05_rate_limit_exceeded_returns_429` | rate_burst="20"; requests="21" | nfr_pattern | Q6/NP-03 |
| 3 | `test_fr05_429_response_includes_retry_after_header` | rate_burst="5"; requests="6" | validation | Q2 |
| 4 | `test_fr05_bucket_refills_over_time` | rate_per_sec="5.0"; wait_s="1.0" | happy_path | Q1 |
| 5 | `test_fr05_healthz_and_readyz_not_rate_limited` | endpoint="/healthz"; requests="1000" | boundary | Q3 |
| 6 | `test_sec_t05_concurrent_requests_do_not_exceed_burst` | rate_burst="10"; concurrent_requests="50"; state_mode="isolate_per_test" | nfr_pattern | Q6/1c/NP-03 |
| 7 | `test_fr05_rate_bucket_row_lock_serializes_concurrent_updates` | concurrent_requests="50"; rate_burst="10"; state_mode="isolate_per_test" | nfr_pattern | Q6/1b/NP-13 |

**Sub-assertions**:

| rule_id | predicate | applies_to |
|---|---|---|
| AC5-burst-capacity | `rate_burst == "20"` | 1 |
| AC5-requests-exceed-burst | `requests == "21"` | 2 |
| AC5-small-burst-for-retry-after | `rate_burst == "5"` | 3 |
| AC5-concurrent-burst-cap | `rate_burst == "10"` | 6 |

Case 1 disposes AC-5.1 (token-bucket capacity/refill rate); cases 2-3 dispose AC-5.2 (429 + `Retry-After`); case 7 disposes AC-5.3 (DB-persisted bucket, single-transaction row-level lock).

Cases 6-7 declare `state_mode: isolate_per_test` — each test gets its own fresh `rate_buckets` row (function-scoped fixture), so concurrent-load assertions from one test can never leak into another's bucket state.

---

### FR-06: 持久化層與交易邊界 (repository/Session/eager load)

**Classification**: INFRASTRUCTURE, DATA_ENTITY, INTEGRATION
**Active Patterns**: NP-07 (Q6/1b, SAD: `repository.session`)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr06_session_commits_on_success_rolls_back_on_exception` | operation="create_task"; simulate_error="false" | happy_path | Q1 |
| 2 | `test_fr06_session_rolls_back_on_exception` | operation="create_task"; simulate_error="true" | failure | Q2 |
| 3 | `test_fr06_no_string_concatenated_sql_in_repository` | scan_target="03-development/src/taskq_api/repository" | validation | Q2 |
| 4 | `test_fr06_list_query_uses_selectinload_constant_sql_count` | rows="10000" | boundary | Q3 |
| 5 | `test_fr06_connection_pool_size_matches_config` | pool_size="5" | happy_path | Q1 |
| 6 | `test_fr06_db_unavailable_readyz_reports_error` | simulate_db_down="true" | nfr_pattern | Q6/1b/NP-07 |
| 7 | `test_fr06_db_connection_failure_does_not_retry_unboundedly` | simulate_db_down="true"; max_retry_attempts="1" | nfr_pattern | Q6/1b/NP-07 |

**Sub-assertions**:

| rule_id | predicate | applies_to |
|---|---|---|
| AC6-rollback-on-exception | `simulate_error == "true"` | 2 |
| AC6-pool-size-default | `pool_size == "5"` | 5 |
| AC6-bounded-retry-count | `max_retry_attempts == "1"` | 7 |

Cases 1-2 dispose AC-6.2 (per-request `Session`, commit/rollback via context manager); case 3 disposes AC-6.3 (no string-concatenated SQL); case 4 disposes AC-6.4 (eager loading, constant SQL count — see also AC-N1.3); case 5 disposes AC-6.5 (`pool_size`; `pool_pre_ping=True` is the same engine-config assertion). AC-6.1 (service layer MUST NOT hold a `Session` directly) is enforced structurally, not by a case here: NFR-06's layering contract (`test_sqlalchemy_import_forbidden_outside_repository`, Deferred table row 8, AC-N6.1/AC-N6.2) forbids importing `sqlalchemy` — where `Session` is defined — outside `repository/`, so no `service/` code can hold one.

Cases 6-7 live under `tests/integration/` per Step 1b (SAD: `repository.session`). AC-N3.1 (context-manager commit/rollback) is verified by cases 1-2; AC-N3.4 (no unbounded silent retry) by case 7.

---

### FR-07: Schema Migration（Alembic 三步演進）

**Classification**: INFRASTRUCTURE, DATA_ENTITY
**Active Patterns**: NP-10 (Q6, SRS: "persist"/"round-trip")

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr07_migration_round_trip_preserves_data` | revision_sequence="v1,v2,v3"; sample_field="result_json"; db_url="sqlite:////tmp/taskq_test.db" | happy_path | Q1 |
| 2 | `test_fr07_upgrade_head_creates_all_six_tables` | expected_table_count="6" | happy_path | Q1 |
| 3 | `test_fr07_downgrade_base_drops_all_tables_exit_0` | expected_table_count_after_downgrade="0" | happy_path | Q1 |
| 4 | `test_fr07_v3_migrates_result_json_into_task_results_table` | source_column="result_json"; target_table="task_results" | happy_path | Q1 |
| 5 | `test_fr07_downgrade_v3_migrates_data_back_before_dropping_task_results` | target_column="result_json" | happy_path | Q1 |
| 6 | `test_fr07_must_not_use_destructive_drop_table_shortcut_in_downgrade` | forbidden_pattern="op.execute" | negative_constraint | Q8 |
| 7 | `test_fr07_runs_against_real_sqlite_file_not_in_memory` | db_url="sqlite:////tmp/taskq_test.db" | nfr_pattern | Q6/NP-10 |
| 8 | `test_fr07_offline_sql_generation_covers_all_three_revisions` | revision_count="3" | happy_path | Q1 |
| 9 | `test_fr07_migration_failure_rolls_back_to_previous_revision` | simulate_migration_error="true"; expected_revision_after_failure="v2" | failure | Q2 |

**Sub-assertions**:

| rule_id | predicate | applies_to |
|---|---|---|
| AC7-source-column-name | `source_column == "result_json"` | 4 |
| AC7-target-table-name | `target_table == "task_results"` | 4 |
| AC7-three-revisions | `revision_count == "3"` | 8 |
| AC7-migration-failure-stays-at-v2 | `expected_revision_after_failure == "v2"` | 9 |

Cases 4, 5 and 8 dispose AC-7.1 (the three-revision v1/v2/v3 evolution with a working, non-destructive `downgrade` and no data loss); cases 2 and 3 dispose AC-7.2 (`alembic upgrade head` / `downgrade base` both succeed); case 6 disposes AC-7.4 (destructive `DROP TABLE` shortcuts forbidden); case 8 disposes AC-7.5 (migration files covered by offline-SQL-generation tests).

**Properties** — Direction B. FR-07's round-trip is a clean algebraic invariant (AC-7.3):

| property_id | invariant | applies_to |
|---|---|---|
| P7-migration-roundtrip | `upgrade(downgrade(sample_data)) == sample_data` | 1 |

`upgrade`/`downgrade`/`sample_data` are symbolic (not bound to case 1's declared string Inputs) — per protocol this degrades to non-blocking `needs_review`, not an error; it records the universal claim (any sample row, not just case 1's literal values, survives one `downgrade -1` + `upgrade head` cycle unchanged) for the P3 reviewer and the future hypothesis-based property test. `state_mode: isolate_per_test` applies (case 1's `db_url` is a dedicated scratch SQLite file, not shared across tests).

Case 9 disposes AC-N3.6 (migration failure → rollback, DB stays at previous revision).

---

### FR-08: 非同步執行器 (`asyncio.TaskGroup`, drain, timeout)

**Classification**: INFRASTRUCTURE, INTEGRATION
**Active Patterns**: NP-13 (Q6, SRS: "async"), NP-15 (Q6/1b, SAD: `service.runner`)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr08_graceful_drain_waits_for_inflight_tasks` | drain_timeout_s="30.0"; inflight_duration_s="5.0" | happy_path | Q1 |
| 2 | `test_fr08_drain_timeout_marks_task_interrupted` | drain_timeout_s="1.0"; inflight_duration_s="5.0" | failure | Q2 |
| 3 | `test_fr08_concurrency_above_max_queues_new_tasks` | max_concurrent="8"; submitted_tasks="9" | boundary | Q3 |
| 4 | `test_fr08_concurrency_at_max_all_run` | max_concurrent="8"; submitted_tasks="8" | boundary | Q3 |
| 5 | `test_fr08_cancelled_error_propagates_not_swallowed` | simulate_cancel="true" | nfr_pattern | Q6/NP-13 |
| 6 | `test_fr08_task_timeout_kills_subprocess_and_awaits_wait` | task_timeout_s="0.5"; command="sleep 60"; subprocess_mode="in_process"; shared_TASKQ_HOME="false" | nfr_pattern | Q6/1b/NP-15 |
| 7 | `test_fr08_must_not_spawn_unbounded_coroutines_above_max_concurrent` | max_concurrent="8"; submitted_tasks="100" | negative_constraint | Q8 |

**Sub-assertions**:

| rule_id | predicate | applies_to |
|---|---|---|
| AC8-drain-timeout-shorter-than-inflight | `drain_timeout_s == "1.0"` | 2 |
| AC8-concurrency-boundary-above | `submitted_tasks == "9"` | 3 |
| AC8-concurrency-boundary-at-cap | `submitted_tasks == "8"` | 4 |
| AC8-timeout-below-sleep-duration | `task_timeout_s == "0.5"` | 6 |

Cases 1-2 dispose AC-8.1 (`TaskGroup`-managed drain, `TASKQ_DRAIN_TIMEOUT`, `interrupted` marking); cases 3, 4 and 7 dispose AC-8.2 (`TASKQ_MAX_CONCURRENT` cap, queue rather than spawn unbounded); case 6 disposes AC-8.3 and AC-N3.5 (reliable `kill()` + `await wait()`, no orphan); case 5 disposes AC-8.4 and AC-N3.3 (`CancelledError` propagates, not swallowed).

Case 6 is the same underlying mechanism as FR-02 case 13 (both exercise `service.runner`'s kill+wait path); declared separately here because FR-08 owns the `TaskGroup`/drain lifecycle (SAD §2.3) while FR-02 owns the HTTP-triggered run. No duplicate test is intended — P3 may implement both assertions in one test file, but each FR keeps its own citable row per Step 1d.

---

### FR-09: 健康檢查與可觀測性 (`/healthz`, `/readyz`, `/v1/metrics`)

**Classification**: API_ENDPOINT, INTEGRATION
**Active Patterns**: NP-07 (Q6/1b, SAD: `repository.session`)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr09_healthz_returns_200_ok` | process_alive="true" | happy_path | Q1 |
| 2 | `test_fr09_readyz_returns_200_when_db_and_migration_ready` | db_reachable="true"; migration_at_head="true" | happy_path | Q1 |
| 3 | `test_fr09_readyz_returns_503_when_db_down` | db_reachable="false" | nfr_pattern | Q6/1b/NP-07 |
| 4 | `test_fr09_readyz_returns_503_when_migration_behind_head` | migration_at_head="false" | failure | Q2 |
| 5 | `test_fr09_metrics_endpoint_requires_admin_scope` | api_key_scope="write"; endpoint="/v1/metrics" | validation | Q2 |
| 6 | `test_fr09_metrics_reports_status_counts_and_latency_percentiles` | endpoint="/v1/metrics" | happy_path | Q1 |
| 7 | `test_fr09_metrics_response_excludes_db_connection_string` | db_url_contains_password="true" | negative_constraint | Q8 |

**Sub-assertions**:

| rule_id | predicate | applies_to |
|---|---|---|
| AC9-readyz-db-down | `db_reachable == "false"` | 3 |
| AC9-readyz-migration-behind | `migration_at_head == "false"` | 4 |
| AC9-metrics-scope-check | `api_key_scope == "write"` | 5 |

Case 1 disposes AC-9.1 (`/healthz` → 200 when alive); cases 2-4 dispose AC-9.2 and AC-9.4 (`/readyz` 200 when DB+migration ready, 503 naming which check failed when either is not — fail-closed); case 6 disposes AC-9.3 (`/v1/metrics` counts + latency percentiles); case 7 disposes AC-N4.2 (no DB connection string in `/v1/metrics`).

---

### FR-10: 錯誤契約 (RFC 7807 `problem+json`)

**Classification**: SECURITY_CONTROL, INFRASTRUCTURE
**Active Patterns**: NP-08 (Q6/1c, SEC: T-04), NP-09 (Q6/1c, SEC: T-06 + SRS: "log"/"trace")

Rule 1 note (v2.13.0): the error-code mapping (SPEC.md §7 / AC-10.5) enumerates 8 distinct status→type pairs. Per the multi-scenario rule, these are 8 separate sub-rows (cases 6-13), not one row bundling 8 codes.

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr10_error_response_is_problem_json` | endpoint="/v1/tasks/unknown-id"; expected_content_type="application/problem+json" | happy_path | Q1 |
| 2 | `test_fr10_error_body_has_required_rfc7807_fields` | endpoint="/v1/tasks/unknown-id" | happy_path | Q1 |
| 3 | `test_sec_t04_secrets_redacted_from_response_and_logs` | task_stdout="postgres://user:pw@host/db leaked"; expected_marker="[REDACTED]" | nfr_pattern | Q6/1c/NP-08 |
| 4 | `test_sec_t06_correlation_id_present_and_logged` | endpoint="/v1/tasks" | nfr_pattern | Q6/1c/NP-09 |
| 5 | `test_fr10_500_error_detail_excludes_stack_sql_path` | simulate_internal_error="true" | negative_constraint | Q8 |
| 6 | `test_fr10_error_code_422_on_validation_failure` | status_code="422"; error_type="/errors/validation" | happy_path | Q1 |
| 7 | `test_fr10_error_code_401_on_missing_key` | status_code="401"; error_type="/errors/unauthenticated" | happy_path | Q1 |
| 8 | `test_fr10_error_code_403_on_insufficient_scope` | status_code="403"; error_type="/errors/forbidden" | happy_path | Q1 |
| 9 | `test_fr10_error_code_404_on_unknown_resource` | status_code="404"; error_type="/errors/not-found" | happy_path | Q1 |
| 10 | `test_fr10_error_code_409_on_name_conflict` | status_code="409"; error_type="/errors/conflict" | happy_path | Q1 |
| 11 | `test_fr10_error_code_429_on_rate_limited` | status_code="429"; error_type="/errors/rate-limited" | happy_path | Q1 |
| 12 | `test_fr10_error_code_503_on_not_ready` | status_code="503"; error_type="/errors/not-ready" | happy_path | Q1 |
| 13 | `test_fr10_error_code_500_on_internal_error` | status_code="500"; error_type="/errors/internal" | happy_path | Q1 |
| 14 | `test_fr10_cancelled_error_not_mapped_to_500` | simulate_cancel="true" | negative_constraint | Q8 |

**Sub-assertions**:

| rule_id | predicate | applies_to |
|---|---|---|
| AC10-content-type | `expected_content_type == "application/problem+json"` | 1 |
| AC10-redaction-marker | `expected_marker == "[REDACTED]"` | 3 |
| AC10-code-422-type | `error_type == "/errors/validation"` | 6 |
| AC10-code-401-type | `error_type == "/errors/unauthenticated"` | 7 |
| AC10-code-403-type | `error_type == "/errors/forbidden"` | 8 |
| AC10-code-404-type | `error_type == "/errors/not-found"` | 9 |
| AC10-code-409-type | `error_type == "/errors/conflict"` | 10 |
| AC10-code-429-type | `error_type == "/errors/rate-limited"` | 11 |
| AC10-code-503-type | `error_type == "/errors/not-ready"` | 12 |
| AC10-code-500-type | `error_type == "/errors/internal"` | 13 |

Case 1 disposes AC-10.1 (`application/problem+json` on every non-2xx response); case 2 disposes AC-10.2 (the six required RFC 7807 body fields); case 5 disposes AC-10.3 (no stack/SQL/path in `detail`); case 4 disposes AC-10.4 (`correlation_id` in header and logs).

---

## Interface Coverage Cross-Check (Agent B checklist item)

All 7 `/v1/*` routes (SAD.md §2.3) sit behind the single `api/dependencies.py` dependency (AC-4.3), so NP-01/NP-02 are verified once at the dependency level rather than duplicated per route:

| Pattern | Blanket coverage (every `/v1/*` route) | Per-route example also declared |
|---|---|---|
| NP-01 (401) | `test_fr03_missing_api_key_returns_401` (AC-3.1: "every `/v1/*` endpoint") | FR-01/FR-02 happy-path cases all supply `api_key_scope`, implicitly proving the auth dependency runs first |
| NP-02 (403) | `test_fr04_all_v1_routes_use_shared_scope_dependency` (route-introspection, AC-4.3) | `test_fr04_insufficient_scope_returns_403` (DELETE), `test_fr09_metrics_endpoint_requires_admin_scope` (GET /v1/metrics), `test_fr04_write_scope_cannot_call_admin_metrics_endpoint` |
| NP-04 (422) | per-endpoint (validation is `TaskCreate`-model-specific, not shared middleware) | FR-01 cases 5, 6, 9-11, 14 (`POST /v1/tasks`) |
| NP-12 (pagination) | `GET /v1/tasks` only (the one interface with a list contract) | FR-01 cases 12-16 |

`/healthz` and `/readyz` are the two documented exceptions (FR-03 AC-3.5, FR-05 AC-5.4) and are explicitly asserted exempt (FR-03 case 7, FR-05 case 5) rather than silently omitted.

---

## Integration-Level NFR Test Cases (TEST_SPEC is the verifier)

> Per Step 1d: these NFR ACs are *not* covered by the harness dimension gate
> (see each NFR's *Coverage note* in SRS.md §4) but *are* verifiable as a
> concrete request/response case — so they get their own `### NFR-XX:`
> section (same shape as an FR section, same parser) rather than a
> `Deferred:` line.

### NFR-01: 效能與查詢效率 (p95 latency, N+1 forbidden)

**Classification**: INTEGRATION
**Active Patterns**: NP-06

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_nfr01_get_task_by_id_p95_within_30ms_at_10k_rows` | rows="10000"; p95_budget_ms="30" | nfr_pattern | Q6/NP-06 |
| 2 | `test_nfr01_list_tasks_p95_within_80ms_at_10k_rows` | rows="10000"; p95_budget_ms="80"; limit="50" | nfr_pattern | Q6/NP-06 |
| 3 | `test_nfr01_list_tasks_sql_statement_count_constant_at_10k_rows` | rows="10000"; expected_sql_statement_count="1" | nfr_pattern | Q6/NP-06 |

**Sub-assertions**:

| rule_id | predicate | applies_to |
|---|---|---|
| N1-perf-budget-get-by-id | `p95_budget_ms == "30"` | 1 |
| N1-perf-budget-list | `p95_budget_ms == "80"` | 2 |
| N1-n1-guard-constant-count | `expected_sql_statement_count == "1"` | 3 |

Case 3 (`N1-n1-guard-constant-count`) disposes AC-N1.3 (constant SQL-statement count, N+1 forbidden — same mechanism as FR-06 case 4 / AC-6.4).

Measured via `pytest-benchmark` (AC-N1.4) at 10,000 seeded rows, per SPEC.md §8 rows 14-15.

### NFR-02: HTTP 與資料層安全 (CORS default-deny — the one AC-N2.x not owned by an FR)

**Classification**: INFRASTRUCTURE
**Active Patterns**: none new (AC-N2.1 is disposed by the Deferred table below; AC-N2.2 by FR-06 case 3; AC-N2.3 by FR-03 case 5/8; AC-N2.4 by FR-04 case 2/3; AC-N2.5 by FR-10 case 5; AC-N2.7 by the Deferred table below; AC-N2.6, CORS, has no owning FR — `config.py` is an independent leaf module)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_nfr02_cors_denies_all_origins_by_default` | cors_origins_env=""; request_origin="https://evil.example" | negative_constraint | Q8 |

**Sub-assertions**:

| rule_id | predicate | applies_to |
|---|---|---|
| N2-cors-default-empty | `cors_origins_env == ""` | 1 |

### NFR-10: 整合覆蓋 (≥80% integration coverage, mandatory scenario checklist)

**Classification**: INTEGRATION
**Active Patterns**: none (line-coverage percentage is a project-wide `pytest-cov` gate; AC-N10.3's scenario checklist is what TEST_SPEC owns, per SRS's own *Coverage note*)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_nfr10_integration_suite_covers_crud_errors_migration_ratelimit_drain` | error_codes="401,403,404,409,422,429,503"; scenarios="crud,migration_roundtrip,rate_limit_trigger_recovery,graceful_drain" | happy_path | Step 2.5 |

**Sub-assertions**:

| rule_id | predicate | applies_to |
|---|---|---|
| N10-seven-error-codes | `len(error_codes.split(",")) == 7` | 1 |
| N10-four-scenarios | `len(scenarios.split(",")) == 4` | 1 |

This case is a checklist assertion (every listed FR/NFR case above already implements each of the 7 codes / 4 scenarios individually); it exists to fail loudly if a future refactor silently drops one of AC-N10.3's mandatory items from the actual integration run (`pytest 03-development/tests/integration`), independent of the aggregate coverage percentage.

AC-N10.1 (≥80% integration-line coverage) is the project-wide `pytest --cov` gate itself (SPEC.md §8 row 3) — a harness dimension measurement, not a discrete TEST_SPEC case, recorded here rather than left silent. AC-N10.2 (driven only via `httpx.AsyncClient(transport=ASGITransport(app))`, never a direct handler call) is the methodology every case in this catalog already follows — see the Cross-Cutting `test_app_entrypoint_persists_task_through_full_stack` case, which states the transport explicitly.

### NFR-12: 系統驗證目標 (`make verify-system`)

**Classification**: INFRASTRUCTURE
**Active Patterns**: none (system-level Makefile target, not an NP-pattern)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_nfr12_verify_system_chains_four_steps_and_prints_pass` | steps="alembic_upgrade,test_suite,health_smoke,downgrade_upgrade_roundtrip"; expected_stdout="verify-system: PASS"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false" | happy_path | Step 2.5 |

**Sub-assertions**:

| rule_id | predicate | applies_to |
|---|---|---|
| N12-four-step-chain | `len(steps.split(",")) == 4` | 1 |
| N12-pass-banner-present | `"PASS" in expected_stdout` | 1 |

`subprocess_mode: out_of_process` — this is the one case that genuinely spawns the real `uvicorn` service and shells out to `alembic`/`pytest` as separate OS processes (SAD.md §1.2); its harness (a `Makefile`-invoking test, or a direct `make verify-system` shell call from CI) MUST propagate `PYTHONPATH` to the child process env — pytest's `pythonpath` config option does not inherit into a subprocess.

Case 1 disposes both AC-N12.1 (the four chained steps: `alembic_upgrade`, `test_suite`, `health_smoke`, `downgrade_upgrade_roundtrip`) and AC-N12.2 (exit 0 + the `verify-system: PASS` banner).

---

## Cross-Cutting Test Cases

### Infrastructure & Middleware Integration (MANDATORY — DB is a real infrastructure component)

| # | Test Function | Type | Derivation |
|---|---|---|---|
| 1 | `test_app_entrypoint_persists_task_through_full_stack` | interface_contract | Step 4 |

`test_app_entrypoint_persists_task_through_full_stack`: starting from `app.py` (the ASGI entrypoint) via `httpx.AsyncClient(transport=ASGITransport(app))`, `POST /v1/tasks` then `GET /v1/tasks/{id}` on the *same* app instance and assert the row round-trips through the real SQLAlchemy/SQLite stack — proves the DB is physically wired end-to-end, not merely mocked at the repository boundary.

### NFR Integration

System-wide NFR verification lives in the dedicated `### NFR-01` / `### NFR-02` / `### NFR-10` / `### NFR-12` sections above (they apply across the whole system, not to one FR, but are written as first-class citable sections rather than nested here to keep their Inputs/Sub-assertions self-consistency-checkable by the same P2 engine). See also the Deferred table below for the Unit/Static NFR checks (documentation, licensing, layering, mutation, assertion-quality, readability) that no request/response case can verify.

### Backward Compatibility

Omitted. NP-11 was evaluated and NOT activated (see NFR Pattern Activation table) — `taskq-api` has no phase-N-1 HTTP contract to preserve (Round 1 `taskq-plus` is a CLI with no API surface), so `test_phase{N-1}_contract_satisfied_in_phase{N}` has no referent. Per Step 4, this section is deleted rather than left as an empty placeholder.

### Deployment Smoke

| # | Test Function | Type | Derivation |
|---|---|---|---|
| 1 | `test_app_starts_and_health_endpoint_returns_200` | smoke | deployment |
| 2 | `test_env_example_declares_all_twelve_taskq_vars` | smoke | deployment (AC-C4.1, `.env.example` §5.1) |

---

## Deferred to Downstream Phases

> Unit/Static-verified NFR ACs (per Step 1d): no request/response case exists
> for these — they are read by a static tool (docstring scanner,
> `pip-licenses`, `import-linter`, `mutmut`, `radon`, `pytest -q`'s own
> summary line) rather than exercised through the ASGI transport. Every row's
> `Inputs` cell declares the AC's numeric threshold as `key="value"` (Round
> 87 站3) so P3 must read the number from this table rather than retype it.
> `test_security_example` (row 1) is `TEST_INVENTORY.yaml`'s own
> `TC-N02-01` entry (layer: static) — preserved verbatim per the NFR
> Layering Hard Rule.

| # | NFR | Test Function | Layer | Inputs | Title |
|---|---|---|---|---|---|
| 1 | NFR-02 | `test_security_example` | static | grep_hits="0" | AC-N2.1: `shell=True`/`eval(`/`exec(` grep gate, 0 hits (`TC-N02-01`) |
| 2 | NFR-02 | `test_bandit_zero_high_zero_medium` | static | high_findings="0"; medium_findings="0" | AC-N2.7: `bandit -r` 0 HIGH / 0 MEDIUM |
| 3 | NFR-03 | `test_no_bare_except_or_except_pass_patterns` | static | violations="0" | AC-N3.2: no bare `except:` / `except Exception: pass` |
| 4 | NFR-04 | `test_redaction_regex_matches_secret_patterns` | unit | redacted_lines="1" | AC-N4.1: secret-pattern regex replaces whole matching line with `[REDACTED]` |
| 5 | NFR-04 | `test_api_key_plaintext_never_persisted_to_disk` | unit | persisted_occurrences="0" | AC-N4.3: plaintext key never written to any persistent location |
| 6 | NFR-05 | `test_docstring_coverage_cites_fr_or_nfr_id` | static | coverage_pct="100" | AC-N5.1: 100% public docstring coverage citing `[FR-XX]`/`[NFR-XX]` |
| 7 | NFR-06 | `test_lint_imports_exit_zero` | static | exit_code="0" | AC-N6.3: `lint-imports` exits 0 |
| 8 | NFR-06 | `test_sqlalchemy_import_forbidden_outside_repository` | static | violations="0" | AC-N6.1/AC-N6.2: layering contract + forbidden `sqlalchemy` import |
| 9 | NFR-06 | `test_importlinter_config_not_deleted_or_downgraded` | static | contract_entries="2" | AC-N6.4: anti-gaming clause (no `.importlinter` deletion / wildcard `ignore_imports`) |
| 10 | NFR-07 | `test_dependencies_pinned_with_exact_version` | static | unpinned_deps="0" | AC-N7.1: `requirements.txt`/`requirements.lock` full pin |
| 11 | NFR-07 | `test_pip_licenses_scan_full_tree_allowlist_only` | static | non_allowlisted_licenses="0" | AC-N7.2/AC-N7.3: `pip-licenses --with-system` full dependency tree |
| 12 | NFR-07 | `test_sbom_json_contains_required_fields_per_dependency` | static | required_fields="4" | AC-N7.4: SBOM schema (`name`/`version`/`license`/`direct|transitive`) |
| 13 | NFR-08 | `test_mutation_score_at_least_70` | static | mutation_score_floor="70" | AC-N8.2: `mutmut` score |
| 14 | NFR-08 | `test_mutation_scope_limited_to_service_and_repository` | static | scoped_layers="2" | AC-N8.3: scope restriction documented in `harness_config.json` |
| 15 | NFR-09 | `test_pytest_run_reports_zero_skipped` | static | skipped="0" | AC-N9.1/AC-N9.2: `pytest -q` skipped count |
| 16 | NFR-09 | `test_zero_assertionless_test_functions` | static | zero_assert_count="0" | AC-N9.3: every test function has ≥1 assert |
| 17 | NFR-09 | `test_no_exclusion_flags_used_in_ci` | static | excluded_tests="0" | AC-N9.4: anti-fraud clause (no `--ignore`/`-k`/`--deselect`/`collect_ignore`) |
| 18 | NFR-09 | `test_traceability_matrix_verified_only_after_execution` | static | unexecuted_verified_rows="0" | AC-N9.6: `TRACEABILITY_MATRIX.md` `VERIFIED` status process check |
| 19 | NFR-11 | `test_project_maintainability_index_at_least_80` | static | mi_floor="80" | AC-N11.1: project MI (LLOC-weighted) |
| 20 | NFR-11 | `test_function_cyclomatic_complexity_at_most_10` | static | cc_ceiling="10" | AC-N11.2: per-function CC |
| 21 | NFR-11 | `test_file_and_directory_size_caps` | static | max_file_lines="400"; max_dir_files="15" | AC-N11.3: file/directory size caps |
| 22 | NFR-11 | `test_api_handler_body_at_most_40_lines` | static | max_handler_lines="40" | AC-N11.4: handler body line cap |
| 23 | NFR-05 | `test_openapi_schema_has_summary_and_description_per_endpoint` | static | endpoints_missing_docs="0" | AC-N5.2: every API endpoint has `summary`/`description` in the FastAPI-generated `/openapi.json` |
| 24 | NFR-08 | `test_harness_config_declares_mutation_testing_enabled` | static | mutation_testing_flag="true" | AC-N8.1: `.methodology/harness_config.json` sets `features.mutation_testing: true` |

AC-N9.5 (FR-07's migration tested against a real SQLite file, not a mock) is **not** deferred — it is disposed by FR-07 case 7 (`test_fr07_runs_against_real_sqlite_file_not_in_memory`, `db_url="sqlite:////tmp/taskq_test.db"`), which is Integration-level and already declared above.

---

## Summary

| Metric | Count |
|---|---|
| FRs covered | 10 / 10 (FR-01..FR-10) |
| NFRs covered | 12 / 12 (NFR-01..NFR-12 — 4 with own Integration-level TEST_SPEC sections, 6 fully disposed via FR cross-references, all 12 with every declared AC-id citable per Step 1d) |
| Total test cases (FR + NFR-integration sections, machine-counted) | 102 |
| By type: happy_path | 40 |
| By type: validation | 7 |
| By type: failure | 7 |
| By type: boundary | 11 |
| By type: state_transition | 6 |
| By type: nfr_pattern | 21 |
| By type: negative_constraint | 8 |
| By type: integration | 2 |
| By type: property (Direction B, overlaps case 1, not additional) | FR-07 |
| Cross-Cutting (Infra wiring + Deployment Smoke, outside the 102 above) | 3 (1 interface_contract + 2 smoke) |
| Deferred (Unit/Static NFR) rows | 24 |
| Active NFR patterns applied | NP-01, NP-02, NP-03, NP-04, NP-06, NP-07, NP-08, NP-09, NP-10, NP-12, NP-13, NP-15 (12 of 15; NP-05/NP-11/NP-14 not activated, reasons in Pattern Activation table) |

---

*Generated by: derive_test_cases.md v1.1 | harness-methodology v2.7.0*
